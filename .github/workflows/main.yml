on:
  workflow_dispatch


jobs:
  start:
    name: deploy to staging
    runs-on: ubuntu-22.04

    steps:
    - name: Start
      run: |
        pwd
        ls
        pip install nova/requirnments.txt
        reflex run --env prod
