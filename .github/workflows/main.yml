on:
  workflow_dispatch


jobs:
  start:
    name: deploy to staging
    runs-on: ubuntu-22.04

    steps:
    - name: Start
      run: |
        pip install ./requirnments.txt
        reflex run --env prod
